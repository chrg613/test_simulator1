-how to use:
-install python 3.12
-in terminal at folder of your project:
-python3.12 -m venv venv
-source venv/bin/activate
-source venv/bin/activate
-pip install --upgrade pip
-pip install -r requirements.txt
-python -m spacy download en_core_web_sm

-next you need a project id in the engine.py file
-get that from google cloud by building your own project()
